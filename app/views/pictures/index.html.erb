<p id="notice"><%= notice %></p>

<h1>Top Cities in Florida</h1>

<table>
  <thead>
    <tr>
      <th>Location</th>
      <!-- <th>Description</th> -->
      <!-- <th>Url</th> -->
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @pictures.each do |picture| %>
      <tr>
        <td><strong><%= picture.location %></strong></td>
        <!-- <td><%= picture.description %></td> -->
        <td><%= image_tag picture.url %></td>
        <td><%= link_to 'View More', picture, class: 'btn btn-success btn-sm' %></td>
        <!-- <td><%= link_to 'Edit', edit_picture_path(picture), class: 'btn btn-info btn-sm' %></td> -->
        <!-- <td><%= link_to 'Destroy', picture, method: :delete, data: { confirm: 'Are you sure?' }, class: 'btn btn-danger btn-sm' %></td> -->
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Picture', new_picture_path, class: 'btn btn-default btn-sm' %>
